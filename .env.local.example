# Supabase — project "Pharmacy Scheduler"
NEXT_PUBLIC_SUPABASE_URL=https://wbsqpqyqnfvcbhsdbnzn.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<Supabase → Settings → API → anon key>
SUPABASE_SERVICE_ROLE_KEY=<Supabase → Settings → API → service_role key — server only, never expose>

# Resend — account jamison.west@outlook.com, domain jamisonwest.ai
RESEND_API_KEY=<Resend → API Keys → key starting with re_>
RESEND_FROM_EMAIL=schedule@jamisonwest.ai

# App URL — used in email links. Local dev:
NEXT_PUBLIC_APP_URL=http://localhost:3102
